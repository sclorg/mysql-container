#!/bin/bash

set -o errexit
set -o nounset
set -o pipefail

shopt -s dotglob
echo "---> Installing application source ..."
mv /tmp/src/* ./ 2>/dev/null || true

# Fix source directory permissions
/usr/libexec/fix-permissions ./

# # /var/lib/mysql/data
# if [ -d "/var/lib/mysql/data" ]; then
#     mv /var/lib/mysql /silo
#     ln -s /silo/mysql /var/lib
    
# fi

# # /etc/opt/rh/rh-mysql80/my.cnf
# if [ -f "/etc/opt/rh/rh-mysql80/my.cnf" ]; then
#     mv /etc/opt/rh/rh-mysql80/my.cnf /silo
#     ln -s /silo/my.cnf /etc/opt/rh/rh-mysql80
# fi
